# dwmbar

```bash
# build and install
make install
# uninstall
make uninstall
# clean artifacts
make clean

# build and install locally
make PREFIX=~/.local install
# uninstall locally
make PREFIX=~/.local install

# run
dwmbar

# restart
pkill -USR1 dwmbar
```
